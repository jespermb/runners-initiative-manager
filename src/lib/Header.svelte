<header class="navbar grid justify-items-center pt-0">
  <h1 class="justify-self-center">Runners initiative manager</h1>
</header>

<style>
  h1 {
    text-transform: uppercase;
    font-size: 1.2rem;
    padding: 1rem;
    border: 1px solid #fff;
    /* text-shadow:
      -2px -2px 8px #fff,
      2px 2px 1rem #fff,
      0 0 2px var(--neon-text-color),
      0 0 4px var(--neon-text-color),
      0 0 6px var(--neon-text-color),
      0 0 8px var(--neon-text-color),
      0 0 10px var(--neon-text-color); */
    
    box-shadow:
      0 0 4px #fff,
      inset 0 0 4px #fff,
      0 0 8px var(--neon-border-color),
      inset 0 0 8px var(--neon-border-color),
      0 0 16px var(--neon-border-color),
      inset 0 0 16px var(--neon-border-color);
  }
</style>